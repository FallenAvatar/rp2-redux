package fallenavatar.rp2_redux.item;

import java.util.Set;

import net.minecraft.block.Block;
import net.minecraft.item.IItemTier;
import net.minecraft.item.ToolItem;
import net.minecraftforge.common.ToolType;

// TODO: Make this work

// public class RP2ToolItem extends ToolItem {
// 	private static Set<Block> getEffectiveBlocks(ToolType tt) {

// 	}

// 	public RP2ToolItem(float attackDamageIn, float attackSpeedIn, IItemTier tier, ToolType tt, Properties builder) {
// 		super(1f, 1f, tier, getEffectiveBlocks(tt), builder.addToolType(tt, tier.getLevel()));
// 	}
	
// }
